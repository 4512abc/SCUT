# 编译原理2013春A

## 1. [15 分] 构造最小状态的 DFA
构造以下正则表达式的最小状态 DFA：  
**正则表达式：** $(a|b)^*a$

OK

---

## 2. [20 分] 考虑以下文法（S 是开始符号）
**文法：**
```
S → aAbDe  
A → BSD | ε  
B → SAc  
D → De | ε  
```

1. **[4 分] 重写文法。**
2. **[6 分] 构造文法非终结符的 First 和 Follow 集。**
3. **[3 分] 判断文法是否为 LL(1) 文法，并说明理由。**
4. **[7 分] 构造该文法的 LL(1) 分析表。**

OK

---

## 3. [20 分] 考虑以下文法（E 是开始符号）
**文法：**
```
(1) E → T * E  
(2) E → a  
(3) T → a  
```

1. **[7 分] 构造该文法的 LR(0) 项目 DFA。**
2. **[3 分] 判断该文法是否为 LR(0) 文法。如果不是，描述 LR(0) 冲突。**
3. **[5 分] 构造 SLR(1) 分析表。**
4. **[5 分] 对于输入字符串 $a * a$，展示 SLR(1) 分析器的分析栈和动作。**

---

## 4. [20 分] 给出以下程序对应的三地址代码序列（无需给出翻译的属性文法）
**程序：**
```
Repeat:
    y := y - 1;
    if y > 0 then y := y - x 
    else y := y + x 
    end
Until y > x and y < 100;
write y
```



---

## 5. [25 分] 编译原理相关问题

1. **[5 分] 下表描述了编译器通常包含的阶段。请填写空白处（A 到 E）。**

   ![](https://cdn.jsdelivr.net/gh/BomLook/blog-pic@main/img/202506071526784.webp)

2. **[5 分] 字母表 $\Sigma = \{a, b, c\}$，生成包含恰好一个 $b$ 的所有字符串的正则表达式是什么？**

3. **[5 分] 考虑文法 $G[A]$：**
   ```
   A → aAd | aAb | ε
   ```
   通过左因子分解重写该文法。

   OK
   
4. **[6 分] 考虑以下文法（定义 '&' 为逻辑运算符，类似于 C 语言中的 '&&' 运算符）：**
   
   ```
   E' → E
   E → E & n | n
   ```
   - 为字符串 $n \& n \& n$ 写出最右推导。
   - 写出每个最右句型的活前缀和句柄。
   
   OK
   
5. **[4 分] 考虑以下属性文法：**
   ```
   S → (L) {S.h = L.h × 2}
   S → a {S.h = 1}
   L → L(1), S {L.h = L(1).h + S.h}
   L → S {L.h = S.h}
   ```
   - 为字符串 `(a, a)` 绘制依赖图。
   - 字符串 `(a, a)` 的最终 $S.h$ 值是多少？
   
   OK



