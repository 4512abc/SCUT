# 编译原理2007-2008上学期2005级本科生A

## 1. 填空题 (18%)
a. 通常，编译器由多个阶段组成，分别是：Scanner、_____________、____________________、____________________、Code Generator 和 __________________。
b. 扫描器生成的逻辑单元称为 Tokens。对于现代编程语言，有五种类型的 Token，分别是：______________、_____________、__________________、____________________、______________。
c. 根据以下 C 语言代码片段回答问题：
```c
int gets(char *s)
{
   if (s == NULL)
   {
      printf(“illegal point parameter \n”);
      return -1;
   }
   return 0;
}
```
a) “int” 是一个 ___________________  
b) “printf” 是一个 _________________  
c) “s” 是一个 (an) _________________  
d) “==” 是一个 ____________________  
e) “0” 是一个 _____________________  

d. 一个文法 G 通常包括四个组成部分，分别是：_______________、__________________、_______________、和 ______。

## 2. 术语解释 (12%)
请简要解释以下编译原理领域的术语：
a) **Compiler** ——  
b) **Source code** ——  
c) **Scanner** ——  
d) **Tokens** ——  
e) **Terminal symbol** ——  
f) **Ambiguous Grammar** ——  

## 3. 扫描 (20%)
(a) (10%) 构造一个 NFA，识别由以下正则表达式定义的语言：  
$$
(a*ba*b)*ba*
$$
![](https://cdn.jsdelivr.net/gh/BomLook/blog-pic@main/img/202506071550121.webp)

(b) (10%) 使用子集构造法，将 NFA 转换为 DFA。  
已知的转换表如下：

| 状态         | a    | b    |
| ------------ | ---- | ---- |
| A={0,1,2,5}  | B    | C    |
| B={2}        | B    | D    |
| C={3,6, 7,8} | E    | F    |
| D={3}        | D    | F    |
| E={3,7, 8}   | E    | F    |
| F={1,2,4,5}  | B    | C    |

OK

## 4. LL 分析 (25%)

考虑以下文法 $G(S)$：
```
S → number | List  
List → (Seq)  
Seq → Seq, S | S  
```
其中，`number` 和 `,` 是终结符，其他符号是非终结符。

(a) (7%) 请写出句子 “(4, (34))” 的最左推导。

(b) [8%] 请通过消除左递归，将文法 $G$ 转换为 LL(1) 文法 $G1$。

(c) [10%] 基于你的 LL(1) 文法 $G1$，请计算 FOLLOW 集和 FIRST 集，并构建 LL(1) 分析表。

OK

## 5. LR 分析 (25%)
考虑以下文法 $G[S]$：
```
E → (L) | a  
L → EL | E  
```
其中，`a`、`(` 和 `)` 是终结符，其他符号是非终结符。

(a) [5%.] 构造该文法的 LR(0) 项目 DFA。

(b) [10%] 构造 SLR(1) 分析表。

(c) [10%] 展示 SLR(1) 分析器对输入字符串 “(a (a a))” 的分析栈和动作。

OK



